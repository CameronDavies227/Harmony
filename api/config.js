const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "Cam",
    password: "root",
    database: "harmony",
  },
  listPerPage: 10,
};
module.exports = config;